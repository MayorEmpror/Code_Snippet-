import React from "react";
export default function LandingPage(props) {
  return (
    //coming soon
    <div
      className={`bg-slate-100 w-${
        props.mobile ? "full" : "full"
      }  flex h-screen flex-col overflow-hidden`}
    >
      LandingPage |comming soon
    </div>
  );
}
